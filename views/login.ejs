<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-800">
    <div class="grid grid-cols-6">
      <div class="col-span-2 col-start-3">
        <%- include("header.ejs") %>

        <div class="flex mb-3 items-center justify-between">
          <div><h6 class="text-xl">Login to Appointment Application</h6></div>
          <Button class="text-white mx-2 px-5 py-2 rounded bg-pink-600">
            <a href="/">Home</a>
          </Button>
        </div>

        <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <li class="mb-2 text-rose-500"> <%= messages.error[i] %></li>
          <% } %>
        <% } %>

        <form action="/session" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div>
            <label for="emailID" class="py-1 text-xl"
              >Email :</label
            >
            <input
              type="email"
              id="emailID"
              name="email"
              required
              class="w-full text-gray-900 border border-gray-600 rounded px-5 py-2 my-2"
            />
          </div>
          <div>
            <label for="pwd" class="py-1 text-xl"
              >Password :</label
            >
            <input
              type="password"
              id="pwd"
              name="password"
              required
              class="w-full border text-gray-900 border-gray-600 rounded px-5 py-2 my-2"
            />
          </div>
          <div>
            <input
              type="submit"
              value="Sign in"
              class="text-white px-5 py-2 rounded my-1 bg-pink-600"
            />
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
